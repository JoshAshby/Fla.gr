#extends views.regularSkeleton

#def topExtra
<div class="row">
    <div class="span5">
            <h3><i class="icon-tags"></i> Public labels</h3>
    </div>
</div>
#end def
#def body
#if $labels
    #for $label in $labels# <a href="/labels/view/$label"><span class="label label-info">$label</span></a> #end for#
#else
    <h2>Oh no!</h2>
    <p>We couldn't find any labels. Either there are no public flags in our system right now, or no one is using labels!<p>
#end if
#end def
