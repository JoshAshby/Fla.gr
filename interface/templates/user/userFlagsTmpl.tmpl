#extends views.bootstrapSkeleton
#from views.partials.flags.flagsListTmpl import flagsListTmpl as flagsList

#def body
<div class="row">
    <div class="offset2 span6">
            <h2><i class="icon-user"></i> $flagAuthor.username <small>flags</small></h2>
    </div>
</div>
#if $flags
    $flagsList.flagsList($flags, $user, $flagAuthor)
#else
    <div class="row">
        <div class="offset2 span6">
            <div class="page-header">
                <h1>Oh no!</h1>
                <p>$flagAuthor.username doesn't seem to have any public flags right now. Check back later and maybe they'll have something!</p>
            </div>
        </div>
    </div>
#end if
#end def
